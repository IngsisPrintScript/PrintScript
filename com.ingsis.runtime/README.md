Com.ingsis.runtime

Propósito:
- Define el runtime y el entorno de ejecución (entornos globales, entradas y variables).

Clases clave:
- `Runtime`, `DefaultRuntime`, `Environment`, `GlobalEnvironment`.
- `environment/entries` — `VariableEntry`, `FunctionEntry` y factories.

Dónde mirar:
- `src/main/java/com/ingsis/runtime`.

Sugerencia: incluir ejemplos de estado del entorno antes/después de la ejecución.
