Com.ingsis.result.factory

Propósito:
- Proveer fábricas para la creación de objetos `Result` (CorrectResult, IncorrectResult) y patrones para centralizar la creación de resultados.

Clases clave:
- `DefaultResultFactory` — implementación por defecto.
- `ResultFactory` — interfaz para extender la creación de resultados.

Dónde mirar:
- `src/main/java/com/ingsis/result/factory`.

Recomendación: documentar casos de uso y ejemplos de creación de resultados para mejorar la comprensión.
