Com.ingsis.tokens

Propósito:
- Definición de la estructura `Token` y fábricas/constructores para tokens usados por el parser.

Clases clave:
- `Token`, `DefaultToken` — estructura principal de token.
- `factories/TokenFactory`, `factories/DefaultTokensFactory` — creación centralizada de tokens.

Dónde mirar:
- `src/main/java/com/ingsis/tokens`.

Recomendación: incluir ejemplos de tokens y sus campos (tipo, valor, posición) para mejorar la documentación automática.