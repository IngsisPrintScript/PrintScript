Com.ingsis.semantic

Propósito:
- Chequeos semánticos y validación de AST mediante handlers y checkers.

Clases clave:
- `SemanticChecker`, `DefaultSemanticChecker`.
- `checkers/` y `handlers/` — reglas para existencia de variables, tipos y operadores.

Dónde mirar:
- `src/main/java/com/ingsis/semantic`.

Recomendación: documentar reglas comunes y ejemplos de errores semánticos detectados.
