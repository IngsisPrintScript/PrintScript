Com.ingsis.interpreter

Propósito:
- Implementa la lógica de interpretación de programas y los visitors que calculan resultados.

Clases clave:
- `ProgramInterpreter`, `DefaultProgramInterpreter`.
- `visitor/` — `InterpreterVisitor`, strategies para expresiones y funciones.

Dónde mirar:
- `src/main/java/com/ingsis/interpreter`.

Ejemplo: revisar `visitor` para ver cómo se evalúan nodos AST.
