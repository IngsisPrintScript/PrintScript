#!/usr/bin/env bash
set -euo pipefail

echo "[pre-push] Running quick validation..."

# Solo tests relevantes para los cambios (más rápido)
./gradlew test --continue

# Opcional: Verificación básica de calidad de código
./gradlew check

echo "[pre-push] Validation passed! Ready to push."