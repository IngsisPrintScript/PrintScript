Com.ingsis.typer

Propósito:
- Lógica de inferencia y resolución de tipos para expresiones, literales, identificadores y funciones.

Clases clave:
- `TypeGetter` — interfaz/contrato para obtener tipos.
- Implementaciones: `DefaultStringTypeGetter`, `DefaultLiteralTypeGetter`, `DefaultFunctionTypeGetter`, `DefaultExpressionTypeGetter`, `DefaultIdentifierTypeGetter`.

Dónde mirar:
- `src/main/java/com/ingsis/typer`.

Sugerencia: añadir ejemplos de inferencia de tipos y cómo las implementaciones se combinan en el flujo de compilación.
