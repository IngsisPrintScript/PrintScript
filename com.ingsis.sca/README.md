Com.ingsis.sca (Static Code Analyzer)

Propósito:
- Pipeline de análisis estático: handlers, publishers y factories para publicar eventos y reglas.

Clases clave:
- `ProgramSca`, `InMemoryProgramSca`.
- `observer/` — handlers, publishers, factories.

Dónde mirar:
- `src/main/java/com/ingsis/sca`.

Nota: documentar reglas SCA frecuentes y cómo extender publishers para integraciones.
